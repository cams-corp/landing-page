---
interface Props {
  text: string;
  class?: string;
}

const { text, class: className = '' } = Astro.props;
---

<div class={`px-4 py-2 rounded-lg whitespace-nowrap ${className}`}>
  <span class="text-xl font-semibold text-secondary">
    {text}
  </span>
</div>
